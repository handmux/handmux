#!/usr/bin/env node
// Stable JavaScript launcher for Node 20. Product source is compiled to dist before development, tests,
// packing or publishing; production never needs a TypeScript runtime loader.
import '../dist/bin/handmux.js';
